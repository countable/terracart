terracart — Pokémon-Go-style farm game on real map data.
Source of truth: this file. Features below are implemented unless marked [TODO]/[DEFERRED].

CORE
 - mobile web (desktop too), 390x844 viewport centered in browser
 - vector map tiles from OpenFreeMap z14 (OpenMapTiles schema, no API key), IndexedDB cache
 - Phaser 3.87 renders the world
 - Geolocation API for navigation, WASD as fallback (works on localhost / desktop)
 - tap player to lock position (drift protection); tap again to unlock + snap back to GPS
 - camera shows 11x11 cells, 5m each (55m viewport). player fixed at center, world pans
 - graceful network disconnect: 'reconnecting' banner; cached tiles still served from IDB

WORLD GENERATION
 - each MVT tile is rasterized once into a 315x315 grid of 5m cells (one terrain class per cell)
 - cell classes (used internally): grass, forest, sand, water, farmland, residential, park, road, path, building, rock
 - cell colors are flat placeholders for now ([TODO] real tile art from Tileset Spring.png with auto-tile lookup)
 - building polygons → house sprite at centroid
 - forest landcover polygons → scattered Maple Tree sprites (seeded per tile)
 - POI points → chest sprite at the POI location (see CHESTS below)

CREATURES
 - chickens (Chicken Red) spawn seeded-per-tile in grass/farmland/park cells, 4-9 per tile
 - cows (Female Cow Brown) spawn 0-2 per tile in farmland only
 - tap creature within 4m to catch; caught state persists in localStorage; caught count in HUD
 - [DEFERRED] variant chickens (Baby Yellow, Blonde Green) and Male Cow as rare/uncommon spawns

CHESTS (POI-driven, drop existing inventory items only)
 - POI classes mapped to chest sprite (uses Objects/chest.png, 16x16, 2 frames closed/open):
     restaurant/cafe/fast_food/grocery/butcher/ice_cream/alcohol_shop/beer/bakery/shop → "food" chest
     park/garden/playground/pitch → "herb" chest
     pharmacy/hospital/dentist → "potion" chest
     attraction/museum/library/town_hall → "rare" chest
     place_of_worship/school/college → "lore" chest
   (Subkind is flavor only — same loot table.)
 - tap chest within 4m + within 18m of player to open
 - chest gives 1-2 of a random EXISTING inventory item (seeds + tools); no new resource types
 - opened chest stays open and dimmed; opened state persists
 - real POI name shown in flash text (e.g. "🍱 → 🥕×2  Casorso Elementary School")
 - density: ~75 chests per tile (one per useful POI)

INVENTORY
 - infinite list, scrolled in pages of 5 slots with ◀ ▶ buttons (page count shown)
 - tap a slot to select; selection persists
 - inventory contains SEEDS only — tools (hoe, watering can, net, feed) are implicit, always available
 - starter seeds (all infinite): 🥕 Carrot, 🍅 Tomato, 🌽 Corn
 - looted seeds (harvested crops, chest drops) add to inventory with a count badge; existing stacks increment
 - count is null/absent = infinite (starter seeds never deplete)

INTERACTION
 - tapping is reserved for: PLANTING seeds, and (future) BUYING / SELLING at shops
 - all other state changes happen via player intersection (walking over a cell)
 - tap inside the 11x11 viewport (priority order):
     1) creature within 4m → catch
     2) world object within ~4m: chest→open+loot, tree→flavor, house→POI name
     3) tillable cell within 15m with seed selected → plant (cell must already be tilled by walking)
 - tap player → toggle GPS lock (yellow tint when locked)
 - flash text fades upward over 0.9s

ECONOMY (TODO — buying & selling)
 - shop POIs (food/herb/potion/rare/lore chest classes) become MARKETS:
     tap shop → modal lets player BUY items (consumable inventory) or SELL produce for coins
 - currency = coins; persisted in save.coins
 - sell prices = base + small randomness per shop class (e.g. food shops pay 2x for carrots)
 - buy stock = small rotating set of items (seeds, future tools)
 - one-time loot drop (current chest behavior) stays available for non-shop POI types
 - NOT YET IMPLEMENTED — current chests give random seed on tap; replace when economy is added

FARMING LOOP
 - the ONLY manual farming action is PLANTING. tools are implicit; other state changes happen
   automatically as the player walks over the cell.
     1) walk over untilled tillable cell → AUTO-TILL (visual: brown tint)
     2) tap tilled cell with a seed selected → PLANT
     3) walk over planted-but-dry cell → AUTO-WATER (halves remaining growth time)
     4) walk over fully-grown crop → AUTO-HARVEST → +1-3 produce of that crop
 - any tillable cell (non-water, non-building) can be tilled
 - crop grows over 3 stages: 40s unwatered, 20s watered
 - per-cell state in save.tilled (array of "x_y" cell keys) and save.planted (list with .watered_t, .crop, .t)
 - tilled cells render with a brown tint overlay until planted

HUD (debug)
 - top-left: lat, lon, GPS state (wasd|waiting|live|LOCKED)
 - line 2: current MVT tile coords, loaded tile count, caught creatures, planted plot count
 - useful for testing; not styled for production

NOT IN SCOPE / REJECTED
 - fences (proposal dropped; OSM/OpenMapTiles tiles don't expose barrier data; user opted out)
 - house interiors (proposal dropped)
 - new resource item types like wood/stone (rejected — inventory uses only seeds + tools)
 - farm view scene (rejected — caught count in HUD is sufficient for now)
